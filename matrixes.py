# Matrix (used a lot in machine learning and photo recogition)
matrix = [
    [1, 2, 3],
    [2, 4, 6],
    [7, 8, 9]
    ]

# Accesses first list in matrix and then second index value in that list
print(matrix[0][1])